@model IEnumerable<WebApplication2.ModelsView.FlightViewDetail>

@{
    ViewBag.Title = "Allarrived";

    var grid = new WebGrid(source: Model, rowsPerPage: 15);
}

<h2>All arrived</h2>
@grid.GetHtml(tableStyle: "table table-hover")
